import styled from "styled-components";

export const Img = styled.img`
    width: 60px;
    height: 60px;
    transition: transform 0.3s;

    &:hover {
        transform: scale(1.1);
        opacity: 0.9;
    }`